const ModalBackDrop = () => {
  return (
    <div className="fixed top-0 left-0 w-full h-full bg-black bg-opacity-50 z-30 flex justify-center items-center"></div>
  );
};

export default ModalBackDrop;
